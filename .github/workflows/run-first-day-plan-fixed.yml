name: Run fixed first day plan
on:
  push:
    paths:
      - scripts/run_first_day_plan_fixed.py
      - .github/workflows/run-first-day-plan-fixed.yml
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: python3 scripts/run_first_day_plan_fixed.py
      - run: npm install --ignore-scripts --no-audit --no-fund
      - run: npm run check
      - name: Commit patch
        run: |
          if git diff --quiet -- src/admin.ts; then
            echo "No changes to commit"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/admin.ts
          git commit -m "Adiciona planejamento do primeiro dia"
          git push
